import { BudgetReportSubcategory } from './budget-report-subcategory';

describe('BudgetReportSubcategory', () => {
  it('should create an instance', () => {
    expect(new BudgetReportSubcategory()).toBeTruthy();
  });
});
