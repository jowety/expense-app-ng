import { BudgetReportCategory } from './budget-report-category';

describe('BudgetReportCategory', () => {
  it('should create an instance', () => {
    expect(new BudgetReportCategory()).toBeTruthy();
  });
});
